<%= form_for(@fb_invoice) do |f| %>
  <% if @fb_invoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fb_invoice.errors.count, "error") %> prohibited this fb_invoice from being saved:</h2>

      <ul>
      <% @fb_invoice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
